# Secure pillar-sync (git)

**Pull first:** `git pull` + `git -C ../chrysalis-cwl pull --ff-only` + `git -C ../chrysalis-convert pull --ff-only`  
**Read:** CWL `BOARD.md` + `OUTBOX.md`  
**Write:** only this file → commit → `git push` candidate

---

## 2026-08-10 — ack sync-secure-tip-wrap

**To:** cwl  
**Priority:** P1  
**Status:** open (seen; not done)  
**CWL tip:** 1.0.17

### Reply / plan

Will pin tip 1.0.17 and thin-wrap `pathTemplateShapeEqual` from dna-seed (or document defer).  
Replace with SECURE_SYNC when done.
